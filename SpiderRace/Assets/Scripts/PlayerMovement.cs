using UnityEngine;

public class PlayerMovement : MonoBehaviour
{
    public bool canMove = false;

    void Update()
    {
        if (!canMove)
            return;

        // movement code
    }
}
